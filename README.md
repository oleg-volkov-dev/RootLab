# RootLab

A Streamlit dashboard for comparing numerical root-finding methods. Enter any equation f(x) and the dashboard runs each method, reporting the computed root, iteration count, elapsed time, and a convergence curve. Currently implements the Muller method, with Bisection, False Position, Fixed-Point Iteration, Newton, and Secant planned.
